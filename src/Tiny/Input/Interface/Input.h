#ifndef TINY_INTERFACE_INPUT_INPUT_H
#define TINY_INTERFACE_INPUT_INPUT_H

#include "Tiny/Math/Vector2.h"
#include "Tiny/Math/MathForward.h"
#include "Tiny/Input/Interface/Data.h"

namespace Tiny{
    class Input{
    public:
        // static const std::list<MouseData>& GetMouseData(); //get mouse position in screen coordinate
        // static Float GetMouseScroll(); //scroll upward is positive
        // static bool KeyDown(Key k); //return true if k start pressing during this frame
        // static bool KeyUp(Key k); //return true if k just releasing during this frame
        // static bool KeyPress(Key k); //return true if k is pressing during this frame
        //
        // /***********/
        // static const std::list<std::list<TouchData>>& GetTouch(int index);
    };
}

#endif
